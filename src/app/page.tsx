'use client';

import * as React from "react";
import HomePageComponent from "../components/home-page"; // Adjust the path based on your file structure

export default function HomePage() {
  return <HomePageComponent />;
}