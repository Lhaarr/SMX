import React from 'react';
import BullCatalog from '@/components/bullcatalog'; // Adjust the path based on your actual folder structure

export default function CatalogPage() {
  return (
    <div>
      <BullCatalog />
    </div>
  );
}